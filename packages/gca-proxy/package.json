{
  "name": "@liteai/gca-proxy",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun build --compile src/index.ts --outfile dist/gca-proxy keys/api_public.pem keys/api_private.pem",
    "start": "./dist/gca-proxy",
    "typecheck": "tsc --noEmit",
    "lint": "biome check --write .",
    "lint:check": "biome check .",
    "test": "bun test"
  },
  "dependencies": {
    "@google/genai": "^1.41.0",
    "google-auth-library": "^10.5.0",
    "hono": "^4.7.13",
    "jose": "^6.0.11",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@biomejs/biome": "catalog:",
    "@types/bun": "latest",
    "typescript": "catalog:"
  },
  "engines": {
    "bun": ">=1.0"
  }
}
