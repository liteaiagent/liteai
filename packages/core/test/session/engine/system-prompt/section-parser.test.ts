import { describe, expect, it } from "bun:test"
import { SectionParser } from "../../../../src/session/engine/section-parser"

describe("SectionParser", () => {
  it("should be defined", () => {
    expect(SectionParser).toBeDefined()
  })
})
