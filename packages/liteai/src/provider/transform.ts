import * as T from "./transform/index"

export namespace ProviderTransform {
  export const OUTPUT_TOKEN_MAX = T.OUTPUT_TOKEN_MAX
  export const message = T.message
  export const temperature = T.temperature
  export const topP = T.topP
  export const topK = T.topK
  export const variants = T.variants
  export const options = T.options
  export const smallOptions = T.smallOptions
  export const providerOptions = T.providerOptions
  export const maxOutputTokens = T.maxOutputTokens
  export const schema = T.schema
}
