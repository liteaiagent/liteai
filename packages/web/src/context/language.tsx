// Re-export from @liteai/ui/panes for backward compatibility.
// The web app provides its own i18n dictionaries as overrides.

export type { Locale } from "@liteai/ui/panes"
export { LanguageProvider, mergeHostDictionaries, useLanguage } from "@liteai/ui/panes"
